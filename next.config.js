/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/zenchain-article', // 🔁 đổi thành tên repo bạn
  assetPrefix: '/zenchain-article',
};

module.exports = nextConfig;
