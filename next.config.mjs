/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export', // BẮT BUỘC khi deploy GitHub Pages
  // basePath: '/zenchain-article',
  // assetPrefix: '/zenchain-article/',
  // images: {
  //   unoptimized: true, // Để tránh Next xử lý ảnh (vì đang export tĩnh)
  // },
};

export default nextConfig;
